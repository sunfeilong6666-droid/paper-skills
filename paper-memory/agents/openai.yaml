interface:
  display_name: "Paper Memory"
  short_description: "科研项目事实、阅读偏好和写作偏好记录"
  default_prompt: "Use $paper-memory only when routed by paper-ai or when explicitly recording/distilling research memory."
policy:
  allow_implicit_invocation: false

